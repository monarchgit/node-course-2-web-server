{
  "name": "web-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1", 
    "start":"node server.js"
  },
  "scriptsComment": {
    "start": "heroku uses this to launch when deployed. run locally by doing npm start",
      "test":"run automated testing"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "hbs": "^4.0.1"
  }
}
